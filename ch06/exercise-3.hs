(^) :: Int -> Int -> Int
(^) _ 0 = 1
(^) x n = x * (x Main.^ (n - 1))

-- 2 ^ 3
-- = {^を適用}
--    2 * (2 ^ 2)
-- = {^を適用}
--    2 * (2 * (2 ^ 1))
-- = {^を適用}
--    2 * (2 * (2 * (2 ^ 0)))
-- = {^を適用}
--    2 * (2 * (2 * 1))
-- = {*を適用}
-- 8
